import ProxyAgent from 'proxy-agent';
const defaultProxy = process.env.https_proxy || process.env.http_proxy || undefined;
export const buildAgent = (proxy = defaultProxy) => proxy ? new ProxyAgent(proxy) : undefined;
export const buildFetchOptions = (options = {}) => {
  const agent = buildAgent(options.proxy);
  return agent ? {
    agent
  } : undefined;
};
export default buildFetchOptions;